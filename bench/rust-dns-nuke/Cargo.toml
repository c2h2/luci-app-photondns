[package]
name = "rust-dns-nuke"
version = "1.0.0"
edition = "2021"
description = "Multithreaded UDP DNS stress/flood tool (std-only, no deps)"

[[bin]]
name = "dnsnuke"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
